# Domain Context: ML (Standard Module)

This directory contains the standard Python-based Machine Learning models for plant analysis and health prediction.

## Architecture
- **Language**: Python
- **Functionality**: Standard analysis pipelines and diagnostic scripts (non-LSTM).
- **Environment**: Keep dependencies isolated and ensure `.vnev` or similar is used.

## AI Agent Instructions
- Use Python best practices (`PEP 8`).
- Tests should be written and run via `pytest`.
- Avoid touching `moonplants_lstm` when operating within this domain.
