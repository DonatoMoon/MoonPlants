я в процесі цього: ## 12) Рекомендований сценарій MVP
   тестування сервісів між собою
                                                                   
   1. Створити тестовий device+credentials у БД.
   2. Виконати claim через `/api/v1/devices/claim`.
   3. Додати 2–4 рослини з прив’язкою до каналів.
   4. Симулятором відправити measurements batch (із каналами       
   1..4).
   5. Перевірити появу measurements по plant_id.
   6. Викликати `/predictions` і перевірити `predictions` table.   
   7. Викликати `water-now` для plant.
   8. Симулятором забрати `/iot/commands`, виконати ACK.
   9. Перевірити `watering_events` + `last_watered_at`.
   10. Видалити plant і перевірити cascade cleanup. додав вручну   
   пристрій в бд, увімкнув симуляцію, під'єднав пристрій через     
   інтерфейс, під'єднав декілька рослин, бачу що дані в бд
   з'являються симульовані. перед тим як підемо по наступним       
   крокам, я хочу відточити цю частину до ідеалу. зараз я бачу що  
   користувач не може видалити у себе контроллер, я вважаю що це   
   потрібно змінити. нехай користувач може від'єднати контроллер   
   від акаунту, усі зв'язки що мають зникнути зникають, рослини по 
   id лишаються не під'єднані, користувач може за бажанням
   під'єднати контроллер знову (або інший користувач цей самий,    
   тому контроллер має типу як скидатись до заводських), якщо      
   рослини не під'єднані до контроллера позначай їх такими в ui.   
   також додай можливість юзера міняти 2 рослини каналами з        
   правильною обробкою даних щоб нічого не переплуталось. виконай  
   це і чекай наступних інструкцій.



